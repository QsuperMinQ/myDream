// Jenkinsfile (Declarative Pipeline)
pipeline {
    agent {
        docker any
    }
    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }
    }
}